[project]
name = "hgmiss"
version = "1.0.0"
requires-python = ">=3.12"
description = "Code for: Coefficient Heterogeneity Is Not Predictive Heterogeneity, and Simulations of Pattern-Conditional Methods Assume It Is"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
